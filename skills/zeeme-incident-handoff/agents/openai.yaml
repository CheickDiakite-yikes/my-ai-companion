interface:
  display_name: "ZeeMe Incident Handoff"
  short_description: "Generate deterministic handoffs with trace and task-state truth"
  default_prompt: "Use $zeeme-incident-handoff to create a complete incident handoff with evidence, active task/stage state, hypotheses, and ranked next actions."
